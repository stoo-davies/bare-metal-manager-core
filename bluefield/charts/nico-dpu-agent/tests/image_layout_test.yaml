suite: cert directory overrides
templates:
  - daemonset.yaml
tests:
  - it: should mount certs at /opt/forge in init container by default
    set:
      image:
        repository: test
        tag: test
    asserts:
      - contains:
          path: spec.template.spec.initContainers[0].volumeMounts
          content:
            name: nico-certs
            mountPath: /opt/forge

  - it: should mount certs at /opt/forge in main container by default
    set:
      image:
        repository: test
        tag: test
    asserts:
      - contains:
          path: spec.template.spec.containers[0].volumeMounts
          content:
            name: nico-certs
            mountPath: /opt/forge

  - it: should hostPath-mount /opt/forge by default
    set:
      image:
        repository: test
        tag: test
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: nico-certs
            hostPath:
              path: /opt/forge
              type: DirectoryOrCreate

  - it: should fall back to /opt/nico when certsDir is cleared
    set:
      image:
        repository: test
        tag: test
      certsDir: null
    asserts:
      - contains:
          path: spec.template.spec.initContainers[0].volumeMounts
          content:
            name: nico-certs
            mountPath: /opt/nico
      - contains:
          path: spec.template.spec.containers[0].volumeMounts
          content:
            name: nico-certs
            mountPath: /opt/nico
      - contains:
          path: spec.template.spec.volumes
          content:
            name: nico-certs
            hostPath:
              path: /opt/nico
              type: DirectoryOrCreate

  - it: should accept a custom certsDir value
    set:
      image:
        repository: test
        tag: test
      certsDir: /custom/cert/dir
    asserts:
      - contains:
          path: spec.template.spec.initContainers[0].volumeMounts
          content:
            name: nico-certs
            mountPath: /custom/cert/dir
      - contains:
          path: spec.template.spec.volumes
          content:
            name: nico-certs
            hostPath:
              path: /custom/cert/dir
              type: DirectoryOrCreate

  - it: should mount the host LLDP snapshot read-only in the main container
    set:
      image:
        repository: test
        tag: test
    asserts:
      - contains:
          path: spec.template.spec.containers[0].volumeMounts
          content:
            name: lldp-snapshot
            mountPath: /var/lib/nico/lldp
            readOnly: true
      - notContains:
          path: spec.template.spec.initContainers[0].volumeMounts
          content:
            name: lldp-snapshot
      - contains:
          path: spec.template.spec.volumes
          content:
            name: lldp-snapshot
            hostPath:
              path: /var/lib/nico/lldp
              type: DirectoryOrCreate
      - notContains:
          path: spec.template.spec.volumes
          content:
            name: lldpd-socket
      - notContains:
          path: spec.template.spec.volumes
          content:
            name: host-lldpcli
      - notContains:
          path: spec.template.spec.volumes
          content:
            name: host-lib
